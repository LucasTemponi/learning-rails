<h1>Hello world</h1>
<%= form_with model: @user, url: signup_path do |f| %>
  <table>
    <tr>
      <th>Email</th>
      <td><%= f.text_field(:email,:required=>true)%></td>
    </tr>
    <tr>
      <th>Password</th>
      <td><%= f.text_field(:password,:required=>true)%></td>
    </tr>
  </table>
  <button class='mt-8' ><%= f.submit('Criar usuário',class:'border cursor-pointer hover:bg-slate-100')%></button>
<%end%>
